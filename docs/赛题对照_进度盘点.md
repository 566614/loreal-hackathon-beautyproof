# 赛题对照 · 进度盘点（2026-09-23）

> 对照基准：天池赛题原文 https://tianchi.aliyun.com/competition/entrance/532496
> 赛题：欧莱雅第二届美妆科技黑客松 · 赛题2「信任守护师 - 创造者的AI卫士」
> 标签：#多模态 #智能体

---

## 〇、赛题原文要求（摘录，作为对照基准）

**赛题主题**：打造一款懂美妆内容生态的"AI鉴真专家"，帮助识别**种草文章、评论区**与 AI 生成素材中被篡改、拼接或伪造的**文案与图片**。

**任务细则**：
1. 开发**多模态**检测方案，识别所选场景中的合成/篡改/伪造痕迹，并给出**可解释的判定依据**；
2. 构建 **Agent**，结合**风险预警与处理建议**，**通过数据集**展示从识别到决策的**完整闭环**。

**最终产出**：内容核验 Agent —— 判断内容真伪、标出可疑之处并说明判定依据，核验过程清晰。

**时间**：早鸟 10/7 23:59（5 支进决赛 + CIIE 展出）；常规 10/20 23:59（9 支）。
今天 9/23 → 距早鸟 **14 天**，距常规 **27 天**。

---

## 一、代码推到 GitHub 了吗 —— 推了，已核实

用 GitHub REST API 直接从服务器核实的（不信任本地 `origin/main` 引用，它历史上骗过 4 次）：

| 项 | 值 |
|---|---|
| 仓库 | `566614/loreal-hackathon-beautyproof` |
| 远端 HEAD | `394144e` = 本地 HEAD ✅ 完全同步 |
| 推送时间 | 2026-09-23 14:08 |
| 仓库体积 | 24.7 MB / 162 个文件 |
| 分支 | 仅 `main` |
| 协作者 | `566614`（owner）、`liying856`（write） |
| **可见性** | ⚠️ **private** |
| **许可证** | ⚠️ **NONE（仓库未声明 License）** |

近 8 次提交：`394144e` GitHub 调研 → `1bc16ce` CLIP 零训练信号 → `d029c28` 本域微调 AIGC → `176b4f4` 方位结论+四大打磨 → `9f9cc7b` / `f9d8e0d` D6/D7 → `7e9e7bd` → `0ea08aa`。

**线上演示**：https://beautyproof-demo.app.workbuddy.host/ —— HTTP 200，已是最新版（页面内可检索到 `ai_17`、`高风险`、`无法判定`、`TruFor`、`AI 生成`）。

---

## 二、逐条对照：做到了 / 没做到

| # | 赛题原话 | 我们的现状 | 判定 |
|---|---|---|---|
| 1 | 识别……被篡改、拼接或伪造的**图片** | 6 工具（hash/c2pa/ela/ocr/aigc/trufor），图像侧很扎实 | ✅ 强 |
| 2 | 识别……**文案**（种草文章、评论区） | **完全没有**。OCR 只是"抄图上的字"（源码注释明写"OCR 是抄写员，不是事实核查员"） | 🔴 **缺** |
| 3 | **多模态**检测方案 | 全部 6 个工具都只看图 → 实际是单模态 | 🔴 **缺** |
| 4 | 可解释的判定依据 | 人话结论（一句话/为什么/建议/提醒）+ 证据链 + 热力图 + 方位 | ✅ 强 |
| 5 | 构建 **Agent** | 是固定顺序流水线（6 个工具挨个跑完 → 规则引擎），属 workflow 不是 Agent | 🟡 弱 |
| 6 | 结合**风险预警** | 四档：high_risk / suspicious / credible / inconclusive | ✅ 有 |
| 7 | 结合**处理建议** | 每条结论带"建议下一步" | ✅ 有 |
| 8 | **通过数据集**展示完整闭环 | `tools/batch_run.py` + `results/dataset_eval.json`，方向命中 6/6 | 🟡 规模小 |
| 9 | 核验过程清晰 | Web 取证台实时进度 + PDF 报告 + 人工复核工作台留痕 | ✅ 有 |

### 结论一句话
**"图片鉴真"这条腿已经站稳了（6/6 命中、真实素材区分度强、有可解释性、有闭环）；"文案"那条腿一条都没长出来，而赛题把它和图片并列写在主题句里。**

---

## 三、还差什么（按"会不会被评委扣分"排序）

### ✅ P0 · 文案侧 —— 已于 2026-09-23 补齐（8/8 评测通过）

赛题主题句写的是"识别**种草文章、评论区**与 AI 生成素材中被篡改、拼接或伪造的**文案与图片**"。
原来只能回答"这张图是不是假的"，答不了文案侧的问题，标签 #多模态 只兑现了一半。

**已补齐两个工具**（`tools/text_tool.py` + `tools/crossmodal_tool.py`）：

- **文案体检**：查违禁宣称，每条给法律出处（《广告法》第 9/17 条、《化妆品监督管理条例》第 43 条）。
  ⚠️ 刻意**不做**"AI 写作检测"——没有能证明该特征在本域有区分度的数据，就只统计、只提示、不给分数。
- **图文交叉验证**（杀手锏）：文案说"原相机实拍"、图却被判整图 AI 生成 → 硬矛盾。
  这依赖图像侧已能判真假，是只有图文双侧都做才拿得到的证据，通用检测器做不到。

**评测 8/8**（`data/text_cases.json` → `results/text_eval.json`），含 4 条**防误报**样本。
其中踩到的坑值得记：文案写"**非**实物拍摄"本是诚实说明，朴素词表匹配会把"实物拍摄"抠出来反报硬矛盾 —— 已加否定词识别。

最小可行补法（不重训大模型）：加一个文本工具，走统一证据格式，产出三类信号：
- 文案 AI 生成痕迹（困惑度 / 重复度 / 句式特征等统计信号）
- 文案与图上 OCR 文字的**跨模态一致性**（种草文说"亲测 7 天"，图上却写着别的）
- 评论区的刷量/模板化特征

第三条正好能接上我们的强项：**当"文案可疑 + 图片 AI 生成"同时成立时，结论才升级** —— 这是真正的多模态交叉验证，也是别的队不容易做的地方。

### 🔴 P0 · "Agent" 味道不够

赛题标签 **#智能体**，明确写"构建 Agent"。我们现在是"六个工具按固定顺序跑完"。
评委大概率会问：**"你的 Agent 会自己决定下一步查什么吗？"**

不必推翻架构。最小补法是给流水线加一层**条件调度**：
- hash 命中已知原图 → 直接出结论，不用跑 TruFor（省时间，且显得"会判断"）
- AIGC 判为整图 AI 生成 → 跳过 TruFor 的"局部篡改"解读（避免口径打架）
- 图上无文字 → 跳过 OCR 比对
- 结论为高风险 → 自动进人工复核队列

这套"根据中间结果决定下一步"就是评委想看的 Agent 决策过程，而且能顺带把单次鉴定时间砍下来。

### 🟡 P1 · 数据集规模偏小，跨生成器未验证

- 现有：真实美妆图 10 张 + 即梦 AI 图 20 张 + 合成篡改 3 张 + 干净图 2 张
- 赛题要求"通过数据集展示完整闭环" —— 35 张在答辩台上容易被追问
- **所有 AI 图都来自即梦单一生成器**。评委必问"换 Midjourney / SD 行不行"，现在答不了
- 今天做的 GitHub 调研也印证：非即梦的 AI 图是最高优先级的补数据方向

### 🟡 P1 · 提交材料一件都没做

代码仓库有了，但评委真正看的是材料：

| 材料 | 状态 |
|---|---|
| 代码仓库链接 | ✅ 有（但 private，见下） |
| 线上 Demo | ✅ https://beautyproof-demo.app.workbuddy.host/ |
| **演示视频**（3–5 分钟） | ❌ 没有 |
| **作品文档 / 答辩 PPT** | ❌ 没有（仓库里搜 ppt/答辩/演示视频/作品 均 0 命中） |
| 数据集说明 | ✅ `data/REAL_AI_DATASET.md` |

### 🟡 P1 · 仓库是 private，评委看不到

赛题规则第 6 条写"参赛者授权举办方可以将部分优质内容通过开源平台开源"，评审要读代码。
private 仓库直接给链接 = 评委打不开。二选一：转 public，或在提交材料里附完整说明 + 关键代码摘录。

### 🟠 P2 · 两个合规风险（赛题明确要求原创性）

赛题："所有参赛代码、算法模型及方案必须确保原创性，不得侵犯第三方知识产权。"

1. **TruFor 仓库无开源许可证**（`grip-unina/TruFor`，License: NONE）—— 我们已在生产使用。需在材料里写明"仅研究用途 / 已注明出处 / 商用前需获授权"。
2. **我们自己的仓库也没声明 License** —— 补一个 LICENSE（MIT 或 Apache-2.0）即可，顺手的事。

### 🟠 P2 · 组队状态待确认

赛题："每支队伍 1–5 人，**所有团队成员均需报名**"、"**作品提交后将不能修改组队**"。
仓库里只有 2 个协作者（你 + liying856）。之前记录里提到过 4 个队友的天池昵称。
提交前必须确认：天池团队已建好、所有人都完成报名 + 学生认证、队长已指定。

---

## 四、建议的优先级

按"扣分权重 ÷ 工作量"排，先做前两个：

1. ~~**文案/文本工具（P0）**~~ ✅ **已完成**（2026-09-23，8/8）
2. ~~**条件调度层（P0）**~~ ✅ **已完成**（2026-09-23，`tools/planner.py`）
   - 五条调度规则 R1–R5 + 三条后置动作 A1–A3，每条都能说出人话理由
   - 决策日志写进 `agent` 字段，网页有「Agent 决策过程」面板，不是黑箱
   - 顺带建了 `data/known_originals/` 品牌方原图库（指纹命中 = 与官方原图一字不差）
   - 图像侧回归 6/6 保持、文案侧 8/8 保持，调度没有牺牲准确率
3. 演示视频 + 答辩 PPT（P1）—— 决定评委能不能看懂你的好
4. 扩数据 + 拿非即梦 AI 图（P1）—— 堵住必被问的漏洞
5. 仓库转 public 或补材料 + 补 LICENSE（P2）—— 半小时能搞定

**关于早鸟**：还有 14 天。上面 1+2 做完 + 补齐 3，冲早鸟是可行的；但如果只来得及做 1，建议走常规通道（还有 27 天），别为赶早鸟交一个缺腿的版本 —— 早鸟没进会自动转常规，但**提交版本以 10/7 截止时为准，之后不能再改**。

---

## 五、附：本次盘点的核实方式

- 远端状态：GitHub REST API 实时拉取（未采信本地 `origin/main` 引用）
- 线上 Demo：实际 HTTP 请求，200 + 内容关键字校验
- OCR 能力边界：读 `tools/ocr_tool.py` 源码与其 docstring
- 工具清单：读 `tools/pipeline.py` 的 `TOOL_LIST` 与 `TOOL_META`
- 赛题原文：抓取天池页面 532496，摘录主题句 / 任务细则 / 赛程 / 参赛规则原文
