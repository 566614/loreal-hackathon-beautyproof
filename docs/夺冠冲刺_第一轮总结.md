# 夺冠冲刺 · 第一轮总结报告（总协调者模式）

> 日期：2026-09-26｜角色：0（总协调者）｜模式：四维度并行 subagent + 主代理收口复核
> 目标：以「夺取总冠军」为靶，评估现状→拆四维度并行提升→汇总协调→规划下一轮。

---

## 一、已完成的提升（四维度全部交付，已主代理逐项核验真落盘）

### 维度A · 核心实力强化（agent-core + 主代理兜底）
- v4 模型 `models/beautyproof_aigen_v4/`（config.json + model.pt 17MB）已训出并**激活**
  （aigc_tool.model_path() 确认加载 v4，不再是静默回退 v3）。
- 复跑 6 方向评测 → `results/v4_eval.json`：**6/6 命中，档位与预期逐字一致，相对 v3 基线零退化**
  （clean_01/clean_02 不误报；copy_move/splice/text_edit 篡改召回保持；ai_17 high_risk）。
- 诚实留白：训练 agent 中途退出，无训练期 val 记录，config.json 的 val_acc 严守 null、未编造。

### 维度B · 短板补齐：跨生成器零样本验证（agent-crossgen）
- 用 ImageGen 自产 12 张「全新风格」AI 美妆图（仅 held-out、绝不回灌训练）存 `data/ai_cross_native/`。
- 显式 pin v3 逐张测：**召回仅 58.33%（7/12）**。漏检高度聚集——滤镜自拍 0.0023 / 直播截图 0.0004 /
  生活场景 0.0 / 水彩 0.34 / 杂志封面 0.32；棚拍/海报/渲染类 0.9–1.0 全中。
- 结论如实写进 `results/crossgen_eval.json`：**跨生成器能力只对已知风格成立，不能宣称已解决**。

### 维度C · 策略优化：答辩叙事与用户旅程（agent-narrative + 主代理补丁）
- `docs/答辩叙事与用户旅程.md`：主 persona = 达人「发布前验真」（小雅），6 步旅程映射 planner 调度的
  八工具（hash/c2pa/ela/ocr/aigc/trufor/text/crossmodal + 规则引擎 + llm_explainer + roundtable）；
  3 条核心信息；10 条评委问答。
- 主代理已把维度B 的 58.3% 诚实并入 Q2/Q3，消除「一会儿 100% 一会儿 58%」口径雷：旧 5/5 = 已知风格
  held-out，新 58.3% = 全新原生风格，分列不混用。

### 维度D · 实战验证：全链路测试 + 评测透明度页（agent-transparency）
- pytest **11 项全过**；pipeline 抽 4 代表案例（clean_01/copy_move/splice/ai_17）复跑通过 →
  `results/e2e_check.json`。
- `docs/评测透明度.html`：受控集 vs 真实集 vs 跨生成器 58.3% 分列、已知盲区 + 应对、模型/数据来源速览、
  免责声明「分数非法律结论，高风险需人工复核」。所有数字溯源 `results/*.json` 与既有 docs。

### 协调消解（主代理干的活）
1. **v4 缺 config 致命坑**：训练 agent 只存权重漏写 config → v4 实际未生效。主代理补 `config.json`，v4 转正。
2. **v4/v3 权重竞争**：维度B 评测显式 pin v3，规避维度A 训练中途 v4 落盘被抢先加载。
3. **口径冲突**：跨生成器 100% vs 58.3% 已统一进叙事文档，评委追问不掉坑。
4. **两 agent 中途被限流打断**（agent-core 漏 config、agent-validate 零产物）→ 主代理补 config + 重派
   两个 recovery agent 收尾，全程未 stray commit。

---

## 二、当前综合评估（vs 夺冠水平）

| 维度 | 本轮后状态 | 距夺冠差距 |
|---|---|---|
| 多工具取证流水线 | 稳，6/6 受控集命中 | ✅ 已达标 |
| 本域微调 AIGC | v4 激活、受控集零退化 | ✅ 已达标（受控口径） |
| 合规护城河 | 全自产数据 + 可核验披露 + 拒绝 GenImage/COCO/品牌图 | ✅ 最强项，已固 |
| 可解释 + 弃权带 + 多智能体 | llm_explainer/roundtable 写进叙事 | ✅ 加分项成形 |
| 答辩叙事/用户旅程 | persona+旅程+10 问答齐备 | ✅ 从「技术 demo」升级为「商业故事」 |
| 评测透明度 | 透明度页 + 真实/跨生成器分列 | ✅ 已补（防现场翻车） |
| **跨生成器真实泛化** | **58.3%（全新风格）** | ⚠️ 最大已知短板，须讲成架构兜底 |
| **真实分布鲁棒性** | 仍依赖自产精修照，重度滤镜盲区在 | ⚠️ 需自产实拍补 |
| 工程完成度 | 11 单测 + CI 绿 + demo 在线 | ✅ 保底 |

**整体判断**：技术完成度、合规护城河、叙事成熟度三项已拉到「强队」水位；披露严谨度（主动讲 58.3%、66%）
是评委眼里的加分而非减分。**夺冠概率从评估基线（总冠军 10–20%）上修到「中高区间」——前提是第二轮把
真实分布与跨生成器两块用「自产实拍 + TruFor 兜底 + 人工复核」讲实、讲透。不补则仍可能在「现场真图测试」
被更强队反超。

---

## 三、后续计划（第二轮迭代，按 ROI 排序）

1. **【最高 ROI · owner 动作】自产实拍扩充**：按 `docs/自产实拍采集指南.md`，你/队友拍自有欧莱雅产品原图
   （不修图、留 EXIF）→ `data/xhs_real_unlabeled/` → `pseudo_label.py` 伪标注 → `train_aigen_v4.py` 再训。
   **这是把真实照误报与跨生成器 58.3% 从根上压下去的唯一可靠路**，也是「未修图原始参考图」合规来源。
   → 验收：真实集误报重测目标 ≤5%。
2. **跨生成器数据补训**：把维度B 那 12 张（及后续拿到的 MJ/SD/Flux 真图）中**确认 AI 的**并入训练，
   重训 v4，再看跨生成器召回能否从 58.3% 拉起（注意：当前 12 张是 ImageGen 单一生成器，仍非真 MJ/SD）。
3. **v4 val_acc 回填**：下次训练保留训练期日志/最佳权重记录，把 config.json 的 val_acc 从 null 填成真实值。
4. **demo 稳定性 + 答辩彩排**：线上 demo 三层备份（线上/本地/预生成样本）跑通；用叙事文档做 1 次计时彩排。
5. **自证材料归档**：把 `results/v4_eval.json` / `crossgen_eval.json` / `评测透明度.html` 作为附件进答辩包。

---

## 四、新数字速查（答辩可用，全部有出处）
- 受控集 6 方向：100% 命中（v3/v4 一致）。
- 跨生成器：已知风格 held-out 5/5(100%)；全新原生风格零样本 7/12(**58.3%**)。
- 真实世界：早期 v1 在 74 张真实精修照误报 66.2%（49 张）；补 74 张重训后 1.35%；v3 验证集 5.6%；
  真实小红书 56 张实测见 `results/xhs_beauty_test.json`（约 69.6%）。
- 弃权带：按 rule_engine 实测 [0.2,0.8) 仅 1.8%，宁可少判不硬判。
- 模型：本域微调 mobilenetv3_large_100，17MB，纯 CPU 可跑；骨干 ImageNet-1k 仅初始化、机器可核验。
