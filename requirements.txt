# BeautyProof 依赖清单（已锁定实测版本）
# 安装方式（CPU 环境，推荐）：
#   1) 先装 torch / torchvision 的 CPU 版（官方默认 wheel 含 CUDA，体积巨大且本机无 GPU）：
#        pip install torch==2.14.0 torchvision==0.29.0 --index-url https://download.pytorch.org/whl/cpu
#   2) 再装其余依赖：
#        pip install -r requirements.txt
# 注：timm / transformers / huggingface_hub 在国内需走镜像：
#       set HF_ENDPOINT=https://hf-mirror.com

# ---- 深度学习框架（CPU 版）----
torch==2.14.0
torchvision==0.29.0
torch-dct==0.1.6

# ---- 模型与 Hub ----
timm==1.0.29
transformers==5.16.1
huggingface-hub==1.29.0
safetensors==0.8.0

# ---- OCR ----
paddlepaddle==3.2.0
paddleocr==3.7.0

# ---- Web / 工具 ----
Flask==3.1.3
requests==2.34.2
requests-toolbelt==1.0.0

# ---- 数值 / 图像 / 可视化 ----
numpy==2.3.5
opencv-python-headless==5.0.0.93
scikit-image==0.26.0
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.11.2
pillow==12.3.0
protobuf==6.33.4

# ---- 测试 ----
# 单测：pytest tests -q（亦可直接 python tests/test_core.py，无需 pytest）
pytest==9.1.1
