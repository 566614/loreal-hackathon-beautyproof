# BeautyProof · 美妆图取证台

> 面向美妆行业的内容可信度鉴定 Agent：一张图 + 一段文案进来，一份「证据链 + 风险分级 + 人话结论」的报告出去。
> 六种看图、两种读字，外加一层只有图文双侧都做才拿得到的**跨模态交叉验证**。
> L'Oréal Brandstorm Hackathon · Trust & Authenticity Track（赛题 2「信任守护师」）

美妆品牌每天在电商、社交平台遇到大量争议内容——盗图、拼接、改文案、AI 生成素材、
涉嫌违规的种草话术。人工审核慢且主观，专业取证工具又太专业。
BeautyProof 把**八种取证手段**（六种看图 + 两种读字）交给**一个会自己判断的 Agent** 调度，
它决定先查什么、什么可以不用查，并把每一次决定的理由都记下来；
最后汇总定级，输出普通人也能看懂的鉴定报告。

> 赛题原话是「识别……被篡改、拼接或伪造的**文案与图片**」——文案和图片是并列的，
> 所以文案侧不是锦上添花，是补齐缺的那一半。详见最后一章。

## 来源声明 / Provenance

为响竞赛规则对「原创性与知识产权」的要求（规则第 3 条：雷同或无创新贡献 = 全队取消成绩），
本项目已明确区分 **第三方 / 开源改编** 与 **BeautyProof 团队原创** 两部分。
完整清单见 [`docs/SOURCES.md`](docs/SOURCES.md)；第三方归属与许可证汇总见仓库顶层 `NOTICE`（本项目代码以 Apache-2.0 发布，见 `LICENSE`）。

**原创边界一句话**：TruFor（CVPR2023）、PaddleOCR、c2patool、ELA 方法论、以及 airealnet/capcheck/sdxl-detector/clip/timm 等预训练权重均为**被调用的第三方能力 / 兜底模型**；
而**统一八工具证据格式、规则引擎四档判定、本域微调 AIGC 分类器（beautyproof_aigen_v1/v2/v3/v4）、跨生成器验证方案、流水线编排与可解释 Agent、图文交叉验证、报告与护栏、Web/离线 demo** 均为 **BeautyProof 团队原创工作**。

## 数据合规红线（赛题 2 · 原创性与知识产权）

赛题规则第 3 条：使用未经授权的第三方数据集 / 模型训练数据 = 雷同或无创新贡献 = 全队取消成绩。本项目把数据来源当成**硬约束**而非口号，逐条落地：

| 红线 | 处理 | 依据 |
|---|---|---|
| GenImage（CC BY-NC-SA 非商用） | **拒绝** | 20 万奖金属商业场景，与 NC 许可冲突 |
| COCO | **拒绝** | 与美妆域无关 + 人脸隐私 |
| ImageNet 当训练集 | **拒绝** | 仅作骨干初始化权重（config.json 机器可核验披露） |
| 品牌 / 电商产品图（爬取） | **拒绝** | L'Oréal ToS 禁止复制 + 商用；且非团队自有 |
| 任何第三方数据集 / 外部图爬取 | **拒绝** | 全部素材团队自产或 CC0 |

我们**怎么做**才补得上数据：① 团队自拍真实美妆图（阮持样机实拍，含 L'Oréal 产品但仅作自有素材、不对外分发原图）；② 用即梦 / ImageGen 等平台**自产** AI 美妆图（团队自有、可溯源）；③ `tools/make_dataset.py` 现场合成干净 + 三种篡改图；④ `tools/pseudo_label.py` 对自产未标注真实图高置信打标做 self-training。**全程零第三方训练数据。**

（完整清单见 [`docs/训练数据来源清单.md`](docs/训练数据来源清单.md) 与 [`docs/模型来源与训练数据合规声明.md`](docs/模型来源与训练数据合规声明.md)。）

## 四种打开方式

```bash
# 0) 线上演示（无需安装任何东西）：
#    https://beautyproof-demo.app.workbuddy.host/
#    （离线演示版由 tools/build_demo.py 从同一套页面生成，双击 demo/index.html 也能看）

# 1) 本地网页演示（推荐给评委）：拖图进去，实时看六个工具跑完并出结论
./run.sh web/app.py          # 打开 http://127.0.0.1:5000

# 2) 一条命令鉴定一张图（全自动：六个工具 → 规则引擎 → JSON/Markdown 报告）
./run.sh tools/pipeline.py <图片路径>
./run.sh tools/pipeline.py <图片路径> --fast   # 跳过深度学习模型，秒出
./run.sh tools/pipeline.py <图片路径> --pdf    # 额外导出一份 PDF（法务 / 品牌方存档）

# 带上配套文案一起鉴定（种草文 / 评论区）—— 图文双模态，这才是完整形态
./run.sh tools/pipeline.py <图片路径> --text "原相机实拍，无滤镜，七天美白亲测有效"
./run.sh tools/pipeline.py <图片路径> --text-file 种草文.txt

# 开启方案 C 的大模型「人话解读」层（需先准备本地模型，见下方「可选：大模型解读层」）
./run.sh tools/pipeline.py <图片路径> --llm
# 开启方案 B 的多 Agent 圆桌交叉复核（纯结构化角色复核，零额外依赖）
./run.sh tools/pipeline.py <图片路径> --roundtable

# 3) 数据集批量评测（自动算命中率）
./run.sh tools/batch_run.py
```

首次拿到仓库后：权重与数据不随仓库分发（体积/隐私原因），执行下面两条即可完整复原：

```bash
./run.sh tools/setup_trufor.py          # 下载并部署 TruFor 官方代码 + 权重（约 370MB），四项体检要全 OK
./run.sh tools/make_dataset.py          # 现场再造评测数据集（干净图 + 三种篡改图）
```

## 大模型解读层（可选，方案 C 的可解释核心）

默认报告由模板从证据拼装，严谨可审计。若想多一段「大模型人话解读」，
需本地准备一个开源多模态模型（默认 Qwen3-VL-4B-Instruct，Apache-2.0、纯 CPU 可跑）：

```bash
./run.sh tools/download_vlm.py                              # 从 hf-mirror 下 GGUF（约 2.5GB）
ollama create qwen3vl-4b -f models/qwen3vl-4b/Modelfile      # 注册成本地模型
ollama serve                                                # 保证 11434 端口在跑
./run.sh tools/pipeline.py <图片> --llm                      # 报告会多一段「五、大模型辅助解读」
```

模型不可用时 `--llm` 自动降级，不影响主流程。该段仅作理解参考，最终定性仍以规则引擎为准，
且会过 validator 的禁用措辞护栏。

## 许可证与第三方声明

本项目以 Apache-2.0 许可证发布（见 `LICENSE`）。TruFor、PyTorch、timm、PaddleOCR、
Qwen3-VL 等第三方组件的许可证与归属见 `NOTICE`。模型权重与训练数据因体积/隐私原因不随仓库分发，
首次运行按 README 顶部说明从官方源按需获取。

## 它怎么工作

```
                       ┌──────────────────────────────────────────┐
                       │              输入：一张美妆图              │
                       └──────────────────┬───────────────────────┘
        ┌─────────────┬───────────────────┼───────────────────┬─────────────┐
        ▼             ▼                   ▼                   ▼             ▼
   文件指纹        内容凭证             压缩痕迹            文字识别       AI 生成检测
  (hash 比对)     (C2PA 凭证)           (ELA)              (OCR)      (本域微调模型)
        │             │                   │                   │             │
        │             │                   ▼                   │             │
        │             │            篡改痕迹检测（TruFor，CVPR 2023）    │
        │             │         整图篡改分 + 可疑区域定位热力图          │
        ▼             ▼                   ▼                   ▼             ▼
        └─────────────┴───────────────────┬─────────────────────────────────┘
                                          ▼
                   ┌──────────────────────┴──────────────────────┐
                   ▼                                             ▼
              文案体检 text                            图文交叉验证 crossmodal
     违禁宣称词典（逐条给法律出处）          拿文案说的去跟图像侧结论对质
     写作特征只提示、不判定               "说实拍却是 AI 图" = 硬矛盾
                   └──────────────────────┬──────────────────────┘
                                          ▼
                              统一证据格式 {tool, observed,
                               cannot_prove, evidence[]}
                                          ▼
                              ┌────────────────────────┐
                              │  规则引擎（四档定级）    │
                              │  high_risk / suspicious │
                              │  credible / inconclusive│
                              └───────────┬────────────┘
                                          ▼
                         鉴定报告：结论 + 人话解释 + 证据链
                                  + 定位热力图 + 边界声明
```

**关键设计**：每个工具输出统一的证据格式，其中必填一项 `cannot_prove`（它证明不了什么）。
规则引擎只按"被验证过有区分度"的信号定级——例如 AIGC 检测器在实测中对真图/篡改图
都给出 ~0.96 的高分（零区分度），于是它照常检测、照常展示，但**不参与自动定级**。
这条纪律让系统在"看起来很聪明"和"真的可信"之间选择了后者。

## 实测结果（5 张样本，CPU 可跑）

| 样本 | 真值 | TruFor 篡改分 | 系统结论 |
|---|---|---|---|
| clean_01（原图直出） | clean | 0.350 | 无法判定（未误报） |
| clean_02（真实图 JPEG q30 重压缩，模拟多次转发） | clean | 0.411 | 无法判定（**重压未误报**） |
| tampered_01（复制粘贴） | copy_move | 0.995 | **高风险** |
| tampered_02（拼接） | splice | 1.000 | **高风险** |
| tampered_03（改文字） | text_edit | 0.595 | **可疑** |

方向命中 **5/5**（篡改图全部报警、两张干净图都不误报）。
两个值得在答辩时讲的数据点：
- **重压缩只把 TruFor 分从 0.35 推到 0.41**——压缩确实留痕，但系统没有把"画质差"当成"被篡改"，这是取证系统最容易翻车的地方；
- 同一张重压缩图 AIGC 检测器给出 **0.999**（比它对所有其他图的分数都高）——再次印证该模型零区分度，不能参与定级。

TruFor 的定位热力图能把拼接/复制区域精确高亮，是整套系统最有说服力的视觉证据（见 Web 演示）。

## 真实素材上的实测（真实美妆图 10 张 vs 即梦 AI 生成图 20 张）

上面 5 张是合成样本，评审真正关心的是**真实素材**。所以又用真实拍摄的美妆图
和即梦（jimeng）生成的美妆图各跑了一遍（完整数据见 `results/real_vs_ai_eval.json`）：

| 指标 | 真实图（10 张） | AI 生成图（20 张） |
|---|---|---|
| TruFor 篡改分 | **0.126 ~ 0.758** | **0.781 ~ 0.998** |
| 可疑区域占比 | 0.0% ~ 9.6% | 0.4% ~ 0.9%（**都极小**） |
| AIGC 检测分（旧 sdxl-detector） | 0.000 ~ 0.999 | 0.000 ~ 0.999 |
| AIGC 检测分（现用：本域微调模型） | **0.0000** | **0.9975 ~ 1.0000** |

两个结论：

1. **TruFor 在真实素材上区分度很强** —— 两组分数几乎不重叠。
   这与「TruFor 查不出整张纯 AI 生成的图」的官方说法不一致；我们的理解是
   AI 生成图的成像噪声特征与真实相机不同，被模型的 noiseprint++ 分支捕捉到了。
   **要说清楚：这是在本批素材（单一生成器「即梦」）上的实测，换生成器需重新校准。**
2. **AIGC 检测器被真实素材彻底证伪** —— 真实图里有 3 张被它判成 0.99（AI 生成），
   AI 图里反而有 7 张被判成 0.000（真实），两组分数完全重叠。
   所以当时它**只展示、不参与定级**——这不是回避，是有实测依据的取舍。
   （后来换过更强的通用模型也照样失效，最终靠**本域微调**解决，见下一节；现已重新参与定级。）

还观察到一个现象：**「高分 + 可疑区域极小」更像整图 AI 生成；
「高分 + 明确的局部区域」更像被人局部改动。**
但我们**没有**把它做成自动定性——真实局部篡改样本（copy_move）的占比同样能低到 1.5%，
与 AI 图的 0.4%~0.9% 挨得太近，自动判定会误伤。
所以它目前只作为人工复核时的参考线索，系统不会自行下这个结论。

## 补 TruFor 的盲区：整图 AI 生成检测（通用模型为什么不行，我们怎么解决）

TruFor 擅长查「局部被人动过」，但看不出「整张图都是 AI 画的」——这是两个不同的问题，
得单独一个工具。我们为此走了两段路，踩坑过程本身也是结论。

### 第一段：换更强的通用开源模型 —— 失败

先后接入两个开源鉴伪模型，在本批素材上实测（`tools/download_aigen_models.py`）：

| 模型 | 对即梦 AI 图 | 对真实美妆照 | 结论 |
|---|---|---|---|
| AIRealNet（SwinV2-Tiny） | 判成 real（ai_prob ≈ 0.0） | 判成 artificial（0.96） | **基本反向** |
| capcheck（ViT-B） | human ≈ 0.99（ai_prob ≈ 0.01） | human ≈ 0.99 | **完全分不出** |

原因：它们的训练域是艺术图 / 新闻图 / SD 通用图，迁移不到「中文美妆 AI 图」这个细分域。
**这不是调参问题，是域不匹配**——所以继续堆更强的通用模型没有意义。

### 第二段：用本域标注图微调专属模型 —— 成功

既然通用域迁不过来，就用我们自己的标注图训一个：真实美妆照 10 张 + 即梦 AI 图 20 张，
外加 clean / tampered 真实照片，共组成 **AI 类 20 张 vs 非 AI 类 15 张**。
脚本 `tools/train_aigen.py`（MobileNetV3-Large + ImageNet 预训练，纯 CPU，90 秒收敛）：

- 训练集 28 张，验证集 7 张 = **4 张训练时没见过的 AI 图 + 3 张被 PS 过的真实照**
- 验证集 **7/7 全对**
- AI 图 ai_prob **0.9975 ~ 1.0000**；真实照（含被局部篡改的）**0.0000**
- **AI 最低分 0.9975 vs 真实最高分 0.0000 —— 完全分开，零重叠**

这里有个关键点值得单独说：那 3 张 tampered 图是「真实照片被人 PS 过」，
模型正确地**没有**把它们当成 AI 生成——说明它学到的是「是不是整图 AI 生成」，
而不是笼统的「有没有异常」。这正是 TruFor 和它分工的意义：
**TruFor 管「被局部改过」，本工具管「整张就是 AI 画的」。**

### 第三段：跨生成器泛化 —— v3（把「换生成器就失灵」修掉）

第二段的模型有个硬伤：**AI 类几乎全是单一生成器（即梦）的图**。换别的模型生成的美妆图，
召回一度只有 **25%**——等于「只会认一个画师的笔迹」。

v3 的修法（`tools/train_aigen_v3.py`）：新增 `data/ai_cross/`（非即梦 / ImageGen 风格的 AI 美妆图 26 张），
并**单独划分**——只把「训练份」并入 AI 类，**验证份整体留作干净 held-out**，
保证跨生成器指标不是拿训练过的图自欺欺人。真实类不变（real_xhs 74 + 原有 15）。

实测：

- 验证集混淆 TP=9 / FN=0 / FP=1 / TN=17，**整体准确率 0.963**
- **跨生成器（同生成器族）held-out 召回 5/5 = 100%**（v2 基线 25%）—— 注意这是「与训练份同生成器族、不同提示词」的验证，不等于没见过的生成器内核
- 真实类没有回弹：抽样复核 ai_prob = 0.0002 / 0.0473 / 0.0，远低于 0.5 阈值

上线：`aigc_tool.py` 的 `MODEL_PRIORITY` 首位为 `beautyproof_aigen_v3`，v2 保留第二位兜底（回滚只删一项）。

### 第四段：v4（域增强 + 伪标注真实图 + 校准带，当前生产模型）

v3 之后，把「真实世界误报」和「跨生成器泛化」两块短板一起补（`tools/train_aigen_v4.py`）：

- **域增强 `beauty_aug.get_train_transform()`**：更强色彩抖动 / 轻微旋转 / 高斯模糊≈磨皮 / 平台缩放伪影 / JPEG 重压缩 / 轻度噪声，专门覆盖「被重度美颜滤镜处理过的真实美妆图」分布——这正是真实世界误报（小红书真实帖 66% 被 v1 误判）的根因。
- **伪标注真实图 `data/real_extra/`**：用已上线模型对团队自产、未标注的真实美妆图高置信打标，作为 self-training 标准手段并入 REAL 类（数据全部团队自有，无第三方数据集）。
- **决策边界上调**：阳性阈值 0.6 + 0.5 模糊带（aigc_tool 内统一），分数落在模糊带输出「无法判定」而非硬判，减少误报。

实测（生产模型，results/v4_eval.json，6 方向）：

- clean_01 / clean_02（JPEG q30 重压缩）aigc_score 均为 0.0（<0.5，**不误报**）
- tampered_01/02/03 的 trufor_score = 0.9946 / 1.0 / 0.595（均 ≥0.5，**篡改召回保持**）
- ai_17 aigc_score = 1.0，仍为 high_risk
- **6/6 命中、档位 6/6 一致，相对 v3 基线零退化**

跨生成器零样本（诚实口径，results/crossgen_eval_v4.json，同 12 张 ImageGen 原生风格 held-out 图）：

- v3 召回 **58.3%**（7/12）→ v4 **66.7%**（8/12），**+8.3pp**
- ⚠️ 关键提醒：这组仍是 ImageGen **同生成器族**（与训练份同内核、不同提示词/风格），不等于 MJ / SD / Flux 这类真·第三方生成器；换内核仍需重新校准。
- 真实世界鲁棒性：靠域增强 + 伪标注真实图已显著压低误报，但**绝不在训练用过的图上报准确率**，对外一律给「真实世界集」那个数。

上线：`aigc_tool.py` 的 `MODEL_PRIORITY` 首位为 `beautyproof_aigen_v4`，v3 保留兜底（回滚只删一项）。

### 边界（必须说清楚）

- 样本量仍偏小（AI 类 46 张 = 即梦 20 + 跨生成器 26；真实类 89 张），概率值容易饱和到 0/1，属小样本结果。
- **跨生成器的 held-out 验证用的是 ImageGen 风格（与训练份同生成器、不同提示词），尚未覆盖 MJ / SD / Flux 这类真·第三方生成器。** 若手上有这些生成器出的美妆图，放进 `data/ai_cross/` 重跑 `tools/train_aigen_v3.py`，即可把「跨提示」推进为「跨生成器内核」。
- 结论**不能当法律鉴定**用，高风险仍必须人工复核；换素材分布需重新校准。

## 补赛题缺的那一半：文案 + 图文交叉验证

前面所有工具都只在**看图**。但赛题的主题句写的是「识别……被篡改、拼接或伪造的**文案与图片**」，
标签是 **#多模态** —— 只做图像等于只做了一半。所以补了两个工具。

### text · 文案体检：查的是「能不能这么说」，不是「是不是 AI 写的」

我们没有做「AI 写作检测」。原因和图像侧拒绝用 sdxl-detector 是同一条纪律：
**自己没验证过的东西，不给它一个分数。** 一段话像不像 AI 写的，我们手头没有能证明
该特征在本域有区分度的数据，所以不做判定，也不编一个概率。

改做的是**能逐条核对的硬规则**：查文案有没有命中《广告法》《化妆品监督管理条例》
点名的敏感宣称，每条给出法律出处、命中位置和上下文。

| 类别 | 依据 | 例子 | 严重度 |
|---|---|---|---|
| 明示/暗示医疗作用 | 《化妆品监督管理条例》第 43 条 /《广告法》第 17 条 | 治疗、消炎、抗菌、药用 | 高 |
| 绝对化用语 | 《广告法》第 9 条 | 100%有效、永久、绝不反弹、全网第一 | 中 |
| 夸大功效/时限承诺 | 《化妆品监督管理条例》第 43 条 | 七天美白、立竿见影、无效退款 | 中 |

⚠️ **命中 ≠ 违法**。是否构成违法由监管部门认定，这里只做「值得人工核改」的提示。
这一层是**合规风险**，和图像侧的**真伪风险**是两个维度，规则引擎里分开定级、分开措辞。

### crossmodal · 图文交叉验证：这是别家拿不到的证据

通用 AI 检测器只有图或只有字，**看不到跨模态打架**。我们图像侧已经能判真假，
才有资格拿图的结论去跟文案对质：

> 文案拍胸脯说"原相机实拍、无滤镜直出"，图却被判成整张 AI 生成的 ——
> 同一件事两边只能有一边是真的。

这比"某个分数偏高"硬得多，因为它不依赖任何单一模型的阈值。
同理还有：声称"原图直出"却被 TruFor 检出明显篡改痕迹。

**这里踩过一个必须说出来的坑**：文案写「**非**实物拍摄」是在诚实说明图不是实拍，
但朴素词表匹配会把「实物拍摄」抠出来，反过来报一条硬矛盾 —— 好人被判成撒谎。
现在会识别否定词（非/不/没/未/并非…），被挡下的词条也会明写在证据里供人工复核，
不悄悄丢掉。**这类误报比漏报更伤可信度。**

### 文案侧实测（8 条评测集，含 4 条防误报样本）

`data/text_cases.json` → `tools/eval_text.py` → `results/text_eval.json`：

| id | 场景 | 冲突判定 | 系统定级 |
|---|---|---|---|
| tc_01 | 正常真人分享 + 干净图 | none | 无法判定（**未误报**） |
| tc_02 | 明示医疗作用 + 干净图 | none | 可疑（合规风险） |
| tc_03 | 绝对化用语堆 5 处 + 干净图 | none | 可疑（合规风险） |
| tc_04 | 只命中 1 处敏感词 + 干净图 | none | 无法判定（**未误报**） |
| tc_05 | **声称原相机实拍 + AI 生成图** | **hard** | **高风险** |
| tc_06 | 诚实说明"非实物拍摄" + AI 生成图 | none | 高风险（**由 AIGC 给出，不记到矛盾的账上**） |
| tc_07 | **声称原图直出 + 拼接篡改图** | **hard** | **高风险** |
| tc_08 | 自己承认修过图 + 篡改图 | none | 高风险（**由 TruFor 给出**） |

方向命中 **8/8**。tc_01 / tc_04 / tc_06 / tc_08 是故意放进去的**防误报**样本 ——
只放"该报警的"样本能把命中率刷到 100%，但那叫自欺欺人。

## 让它成为 Agent，而不只是流水线

赛题的标签是 **#智能体**。八个工具挨个跑一遍那叫流水线，不叫 Agent ——
真正的区别是：**它会不会根据已经查到的东西，决定下一步做什么。**

所以加了一层决策层（`tools/planner.py`）。它把工具分成三波（快检 → 中检 → 深检），
每跑完一波就重新判断一次「下一波哪些值得跑」，而不是无脑全跑。

### 它凭什么决定（每条规则都能说出人话理由）

| 规则 | 触发条件 | 决定 | 理由 |
|---|---|---|---|
| R1 | 图片真实格式不是 JPEG | 跳过 ELA | ELA 靠「再压一遍 JPEG」找破绽，PNG 转 JPEG 的差异会盖过真实痕迹 |
| R2 | 指纹命中品牌方原图库 | 跳过 ELA + TruFor | 与官方原图一个字节都不差，没被改过，再做痕迹检测没意义 |
| R3 | 没有提供文案 | 跳过文案侧两个工具 | 没有文案就没有文案侧可查 |
| R4 | 图上没识别出文字 | 数字比对跳过 | 没有图上文字，图文数字无从比对 |
| R5 | 已判定整图 AI 生成 | TruFor 只出定位图、不用于定性 | 避免两个模型的结论口径打架 |

**原则只有一条：只跳过「查了也没意义」的，绝不跳过「可能有风险」的。**
省时间是副产品，不是目的 —— 宁可多跑一个工具，不能漏掉一个信号。

### 判完之后它还知道该干什么

| 规则 | 动作 |
|---|---|
| A1 | 高风险 → 自动进入人工复核队列 |
| A2 | 文案命中违规宣称 → 建议核改后再投放 |
| A3 | 发现图文硬矛盾 → 分别核实图片来源与文案出处 |

### 决策过程是可见的，不是黑箱

每一次跳过、每一个动作都写进 `outputs/analysis_*.json` 的 `agent` 字段，
网页上直接展示成「Agent 决策过程」面板：跳过哪个工具、依据哪条规则、理由是什么。
评委问"你怎么证明它是 Agent 不是 if-else"，就把这一页翻给他看。

### 这里踩过一个很隐蔽的坑

第一版写的是「如果 ela 不在证据里，就判断该不该跑」。但证据是从 `outputs/` 目录读的，
里面还留着**上一轮**跑出来的 `ela_xxx.json` —— Agent 以为早就跑过了，R1 规则**一次都没触发过**。

改成以「本次已跑完的工具集合」为准，并在每轮开始前清掉这张图的旧证据
（顺带修掉另一个隐患：某工具本次失败了，judge 却拿上次的旧结果凑数，输出看起来一切正常）。
**判断"跑没跑过"要看本次，不能看磁盘上有什么。**

## 给非技术同事看的那一段（人话结论）

分数环和热力图只有技术同学看得懂，但真正拿这张图去做决策的是品牌方、法务、平台审核——
他们要的不是「0.99 分」，而是「这张图还能不能用、我下一步该干嘛」。
所以每条结论都会额外生成一段人话（一句话结论 / 为什么 / 建议下一步 / 提醒）：

> **发现明显篡改痕迹，建议人工复核**
>
> 取证模型给出的整图篡改分为 1.000（越接近 1.0 越可疑），可疑区域约占全图 2.5%。
> 说明图上很可能有区域被复制、拼接或改写过，配合定位热力图的红色区域可以大致看出是哪里。
>
> **建议下一步**：先不要用这张图对外投放或作为证据；找品牌方要原始原图核对。
> **提醒**：以上结论来自算法比对，不是法律意义上的鉴定意见。

网页、离线 Demo、Markdown 报告三处共用同一套文案（统一出自 `rule_engine.explain()`），
不会出现「网页这么说、报告那么说」的口径打架。

## 目录结构

```
├── web/                Flask 演示服务（拖拽上传 → 实时进度 → 可视化报告）
│   ├── app.py          取证台主服务（上传 / 鉴定 / 看结果）
│   └── review_app.py   人工复核工作台（高风险必人工复核的可操作界面，复核结论留痕）
├── tools/              六个检测工具 + 流水线 + 规则引擎 + 报告生成器
│   ├── pipeline.py     一条命令串起全部工具（Web 与 CLI 共用）
│   ├── rule_engine.py  四档定级裁判（阈值与开关都有实测注释）；
│   │                   explain() 负责把结论翻成非技术同事看得懂的人话（含方位）
│   ├── trufor_tool.py  TruFor 集成（含 Windows 路径修复、定位热力图与方位估算）
│   ├── planner.py       Agent 决策层（分波调度 + 决策日志 + 后置动作）
│   ├── text_tool.py     文案体检（违禁宣称词典，逐条给法律出处）
│   ├── crossmodal_tool.py 图文交叉验证（宣称词 vs 图像侧结论，含否定词识别）
│   └── eval_text.py     文案侧评测（8 条含 4 条防误报样本）
│   ├── export_pdf.py   鉴定报告导出 PDF（中文字体可用，供法务/品牌方存档）
│   ├── batch_review.py 批量审核队列（一天审成百上千张，输出 CSV/JSON/静态看板）
│   ├── robustness_test.py 对抗/鲁棒性测试（回答"攻击者反向调图能不能骗过"）
│   └── ...
├── data/
│   ├── known_originals/ 品牌方已知原图库（指纹命中即证明与官方原图一字不差）
│   ├── dataset_real_ai.json  真实美妆图(10) vs 即梦AI图(20) 评测集，含真值与来源说明
│   └── text_cases.json       文案侧评测集(8条，含4条防误报样本)
├── outputs/            各工具证据 JSON / verdict / TruFor 热力图
├── reports/            人能读的 Markdown 鉴定报告
└── results/            数据集评测汇总
```

## 已知边界（写在最前面，不藏在最后）

- 所有分数来自算法，**不构成法律意义上的鉴定结论**，高风险图必须人工复核。
- AIGC 检测器**早期版本**（sdxl-detector / 通用模型）在真实素材上被证伪：真图会被判成 AI、AI 图会被判成真，
  两组分数完全重叠 → 当时只展示、不参与定级。**现由 v4 本域微调模型接管**（参与定级，阳性阈值 0.6 + 0.5 模糊带），
  但分数仍有饱和、换分布需重新校准，高风险仍须人工复核（依据见「v4」一节实测）。
- TruFor 官方称查不出整张纯 AI 生成的图，但我们在即梦生成的美妆图上实测到它给出高分。
  这属于「能报警、但机制待确认」，且**只在单一生成器的本批素材上验证过**，换来源需重新校准。
- 分数高 ≠ 一定有罪（JPEG 压缩、滤镜也会留痕）；分数低 ≠ 绝对干净。
- **文案侧不做「AI 写作检测」**：写作特征只统计、只提示，未校准为概率，不参与自动定级。
  因为没有能证明该特征在本域有区分度的数据 —— 与其给一个好看但站不住的分数，不如不给。
- 命中违禁宣称 ≠ 违法，是否构成违法由监管部门认定；这是合规维度，与真伪维度分开定级。
- 图文冲突只能说明两边说法不相容，不能判定是谁在造假（也可能是改稿手滑、或文案套错了图）。
- 宣称词表为中文关键词匹配，否定式表达（"非实拍"）已处理，但更复杂的反讽/引号用法仍可能误判。
- 本域微调模型权重 `models/beautyproof_aigen_v4/`（17MB）已随仓库分发（`.gitignore` 显式放行），首次克隆即可直接推理，无需现场训练；v1/v2/v3 权重同理入库。
- 本项目所有素材与阈值都在 README 和代码注释里注明了实测依据，换素材分布需重新校准。

## 团队

阮佳瑶（产品定义 / 素材 / 答辩）× AI 搭档（流水线与模型工程）。
详细开发日志见 `learning/` 与各工具源码注释。
