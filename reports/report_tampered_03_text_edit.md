# 图片鉴定报告：tampered_03_text_edit

- 检测对象：`tampered_03_text_edit.png`
- 参与检测的工具：6 个
- **鉴定结论：可疑（suspicious）**

## 一、结论

> **有可疑迹象，建议人工看一眼**
>
> 取证模型给出的整图篡改分为 0.595，已超过可疑线，可疑区域约占全图 6.4%。不像高风险那样确定，但也不像干净图那样平稳，值得人工核对。
>
> **建议下一步**：暂缓对外投放，人工对照原图确认后再用。
>
> **提醒**：以上结论来自算法比对，不是法律意义上的鉴定意见。分数高不代表一定有罪（压缩、滤镜也会留痕），分数低也不代表绝对干净。

发现了局部篡改迹象，**建议人工重点查看报告标注的可疑区域**。

判定依据（规则引擎给出）：
- AIGC 检测：AI 生成概率 0.8491（≥0.7），但本模型在本批素材上实测零区分度（真实图 0.9571 vs 篡改图 0.9621），仅作参考，不参与自动定级
- TruFor：篡改分数 0.595（≥0.5）→ 有明显篡改痕迹；可疑区域约占全图 6.4%

## 二、检测依据（逐工具）

### 1. 内容指纹（哈希）

- 观察到：这张图的内容指纹是 sha256:cfeb560f5874c84f86b597d7f23541a30799a466c3c6aecc55f53a9ee136efc0，文件大小 615786 字节
- 文件大小：615786 字节

### 2. 内容凭证（C2PA）

- 观察到：c2patool 没能读出凭证（绝大多数网图都没有凭证，属正常）
- 凭证状态：**missing（无凭证）**

### 3. 压缩痕迹分析（ELA）

- 观察到：整图平均误差水平 1.268；误差最高的区域 bbox=[93, 515, 186, 618]，该区域误差 3.935。注意：这张图的真实格式是 PNG，不是 JPEG。非 JPEG 转 JPEG 本身就会产生很大差异，本结果仅作演示、不可信。
- 整图平均误差：1.268
- 最可疑区域：bbox=[93, 515, 186, 618]，误差 **3.935**（触发阈值 2.0）
- 可疑区域总数：3

### 4. 图上文字识别（OCR）

- 观察到：在图上一共读出 2 行文字
- 识别内容：@美妆鉴定局OFFICIAL

### 5. AI 生成检测（AIGC）

- 观察到：AI 生成概率 0.8491（模型给出了较高的 AI 生成概率，但该模型误判率很高（真实图也常被判高分），不能单凭此项下结论）
- AI 生成概率：**0.8491**（触发阈值 0.7）
- 模型标签：artificial

### 6. 篡改痕迹检测（TruFor）

- 观察到：TruFor 篡改分数 0.595（有明显篡改痕迹）；可疑区域约占全图 6.4%
- 整图篡改分数：**0.595**（阈值：≥0.9 高风险，≥0.5 可疑）
- 可疑区域占比：约 6.4%
- 定位热力图：`outputs/tampered_03_text_edit_trufor_heat.png`
- 原图叠加图：`outputs/tampered_03_text_edit_trufor_overlay.png`

## 三、这些工具说不清什么（必读）

以下每一条都是对应工具**明确声明的能力边界**，超出这些范围的结论本报告不作数：

- **内容指纹（哈希）**：哈希只证明文件的身份，不能证明图片真伪、不能判断是否被篡改
- **内容凭证（C2PA）**：没有凭证不等于图片是假的；有凭证也不等于没被恶意编辑过
- **压缩痕迹分析（ELA）**：ELA 只是压缩痕迹分析，不能证明某处一定被篡改；截图、反复保存、平台压缩都会造成同样现象
- **图上文字识别（OCR）**：OCR 只是抄写员，不能证明文字内容为真，也不能证明图片被篡改
- **AI 生成检测（AIGC）**：模型会误判：真人也常被误判为 AI，AI 生成的也可能蒙混过关；分数不是事实
- **篡改痕迹检测（TruFor）**：模型也会误判：分数高不代表一定有罪（压缩/滤镜也会留痕），分数低也不代表绝对干净；且 TruFor 只擅长查「局部被人动过手脚」，官方明确说明它查不出「整张完全由 AI 生成的图」

## 四、建议

1) 人工放大查看可疑区域；2) 尽量找到原始未压缩版本比对；3) 结合图片来源（谁发的、何时发的）综合判断。

---

*本报告由模板从工具证据 JSON 直接拼装，未使用大模型生成，所有结论均可追溯到 `outputs/` 下的原始证据文件。*
