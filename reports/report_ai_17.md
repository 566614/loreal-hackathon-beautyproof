# 图片鉴定报告：ai_17

- 检测对象：`ai_17.png`
- 参与检测的工具：5 个
- **鉴定结论：高风险（high_risk）**

## 一、结论

> **检测到整图由 AI 生成，建议人工复核**
>
> AI 生成检测给出 0.9994（≥0.9），说明这张图很可能是整张由 AI 生成的，而不是相机拍出来的。取证模型 TruFor 同时给出篡改分 0.9962，可疑区域约占全图 0.8%，与「整图 AI 生成」的判断方向一致。
>
> **建议下一步**：先不要用这张图对外投放或作为证据；找品牌方要原始原图核对，或交给专业鉴定机构复核后再定。
>
> **提醒**：以上结论来自算法比对，不是法律意义上的鉴定意见。分数高不代表一定有罪（压缩、滤镜也会留痕），分数低也不代表绝对干净。

检测结果显示该图很可能有问题，**不建议直接采信**，必须人工复核。

判定依据（规则引擎给出）：
- AI 生成检测：AI 生成概率 1.0（≥0.9）→ 极可能是整图由 AI 生成的图

## 二、检测依据（逐工具）

### 1. 内容指纹（哈希）

- 观察到：这张图的内容指纹是 sha256:a27d35ee36c2bc4c1af80b6a55822018496d9fea12b49c4ac09a15ffc5d1de91，文件大小 2500892 字节。该指纹未在品牌方已知原图库中命中（库里没有 ≠ 图有问题，很可能只是还没收录这张原图）。
- 文件大小：2500892 字节

### 2. 内容凭证（C2PA）

- 观察到：文件元数据带 TC260 AIGC 强制标识（Label=1），生成器自声明这是 AI 生成内容（生产者编码 00119144030008867405X210001…）。依据 《人工智能生成合成内容标识办法》（2025-09-01 施行）+ GB/T TC260 AIGC 元数据规范。这是全部工具里证据强度最高的一种：不需要模型推理，直接读文件即可复核。
- 凭证状态：**present（有凭证）**

### 3. 图上文字识别（OCR）

- 观察到：在图上一共读出 2 行文字
- 识别内容：ESSEACE

### 4. AI 生成检测（AIGC）

- 观察到：AI 生成概率 0.9994（模型给出的 AI 生成概率很高（≥0.9），结合其他工具，很可能是整图由 AI 生成的图）
- AI 生成概率：**0.9994**（≥0.9 高风险，≥0.8 可疑）
- 模型标签：ai_generated

### 5. 篡改痕迹检测（TruFor）

- 观察到：TruFor 篡改分数 0.9962（篡改痕迹非常明显）；可疑区域约占全图 0.8%
- 整图篡改分数：**0.9962**（阈值：≥0.9 高风险，≥0.5 可疑）
- 可疑区域占比：约 0.8%
- 定位热力图：`outputs/ai_17_trufor_heat.png`
- 原图叠加图：`outputs/ai_17_trufor_overlay.png`

## 三、这些工具说不清什么（必读）

以下每一条都是对应工具**明确声明的能力边界**，超出这些范围的结论本报告不作数：

- **内容指纹（哈希）**：哈希只证明文件的身份，不能证明图片真伪、不能判断是否被篡改
- **内容凭证（C2PA）**：没有凭证/标识不等于图片是假的；有凭证也不等于没被恶意编辑过；TC260 标识可能被剥离，缺失时不能反推结论
- **图上文字识别（OCR）**：OCR 只是抄写员，不能证明文字内容为真，也不能证明图片被篡改
- **AI 生成检测（AIGC）**：模型也会误判：分数高不代表一定 AI 生成，分数低也不代表一定真实；本项与 TruFor 互补——TruFor 查局部篡改，本项查整图 AI 生成
- **篡改痕迹检测（TruFor）**：模型也会误判：分数高不代表一定有罪（压缩/滤镜也会留痕），分数低也不代表绝对干净；且 TruFor 只擅长查「局部被人动过手脚」，官方明确说明它查不出「整张完全由 AI 生成的图」

## 四、建议

1) 不要直接采信该图；2) 人工核验原始出处；3) 如用于举证，保留本报告与原始证据 JSON。

---

*本报告主体由模板从工具证据 JSON 直接拼装（结论可追溯到 `outputs/` 下的原始证据文件）。若存在「五、大模型辅助解读」段，则为模型基于证据生成的辅助说明，已通过禁用措辞校验，不替代规则引擎结论。*
