# 图片鉴定报告：tampered_02_splice

- 检测对象：`tampered_02_splice.png`
- 参与检测的工具：6 个
- **鉴定结论：高风险（high_risk）**

## 一、结论

> **发现明显篡改痕迹，建议人工复核**
>
> 取证模型给出的整图篡改分为 1.0（越接近 1.0 越可疑），可疑区域约占全图 2.5%。可疑痕迹主要集中在画面中部，很可能是这块区域被复制、拼接或改写过，对照定位热力图的红色区域即可确认。
>
> **建议下一步**：先不要用这张图对外投放或作为证据；找品牌方要原始原图核对，或交给专业鉴定机构复核后再定。
>
> **提醒**：以上结论来自算法比对，不是法律意义上的鉴定意见。分数高不代表一定有罪（压缩、滤镜也会留痕），分数低也不代表绝对干净。

检测结果显示该图很可能有问题，**不建议直接采信**，必须人工复核。

判定依据（规则引擎给出）：
- 图文交叉验证：发现硬矛盾 —— 文案称「原图直出」（上下文 …这瓶是原图直出，没有任何后期，实物…）；而 篡改检测给出 1.0（≥0.9），有明显篡改痕迹。两者只能有一边是真的

## 二、检测依据（逐工具）

### 1. 内容指纹（哈希）

- 观察到：这张图的内容指纹是 sha256:6e4c52b875231d1c4c1037e35130ad45aa98a941af07ad65689fcfa6ba09128d，文件大小 610828 字节。该指纹未在品牌方已知原图库中命中（库里没有 ≠ 图有问题，很可能只是还没收录这张原图）。
- 文件大小：610828 字节

### 2. 内容凭证（C2PA）

- 观察到：c2patool 没能读出凭证（绝大多数网图都没有凭证，属正常） 未检出 TC260 AIGC 标识（注意：标识可能被截图/转格式/重保存剥离，无标识不代表不是 AI 生成）。
- 凭证状态：**missing（无凭证）**

### 3. 压缩痕迹分析（ELA）

- 观察到：整图平均误差水平 1.146；误差最高的区域 bbox=[279, 412, 372, 515]，该区域误差 4.336。注意：这张图的真实格式是 PNG，不是 JPEG。非 JPEG 转 JPEG 本身就会产生很大差异，本结果仅作演示、不可信。
- 整图平均误差：1.146
- 最可疑区域：bbox=[279, 412, 372, 515]，误差 **4.336**（触发阈值 2.0）
- 可疑区域总数：3

### 4. 图上文字识别（OCR）

- 观察到：在图上一共读出 2 行文字
- 识别内容：P

### 5. AI 生成检测（AIGC）

- 观察到：AI 生成概率 0.0（模型认为看起来像真实拍摄 / 人工制作的图）
- AI 生成概率：**0.0**（≥0.9 高风险，≥0.8 可疑）
- 模型标签：real

### 6. 篡改痕迹检测（TruFor）

- 观察到：TruFor 篡改分数 1.0（篡改痕迹非常明显）；可疑区域约占全图 2.5%，主要集中在画面中部
- 整图篡改分数：**1.0**（阈值：≥0.9 高风险，≥0.5 可疑）
- 可疑区域占比：约 2.5%
- 定位热力图：`outputs/tampered_02_splice_trufor_heat.png`
- 原图叠加图：`outputs/tampered_02_splice_trufor_overlay.png`

## 三、这些工具说不清什么（必读）

以下每一条都是对应工具**明确声明的能力边界**，超出这些范围的结论本报告不作数：

- **内容指纹（哈希）**：哈希只证明文件的身份，不能证明图片真伪、不能判断是否被篡改
- **内容凭证（C2PA）**：没有凭证/标识不等于图片是假的；有凭证也不等于没被恶意编辑过；TC260 标识可能被剥离，缺失时不能反推结论
- **压缩痕迹分析（ELA）**：ELA 只是压缩痕迹分析，不能证明某处一定被篡改；截图、反复保存、平台压缩都会造成同样现象
- **图上文字识别（OCR）**：OCR 只是抄写员，不能证明文字内容为真，也不能证明图片被篡改
- **AI 生成检测（AIGC）**：模型也会误判：分数高不代表一定 AI 生成，分数低也不代表一定真实；本项与 TruFor 互补——TruFor 查局部篡改，本项查整图 AI 生成
- **篡改痕迹检测（TruFor）**：模型也会误判：分数高不代表一定有罪（压缩/滤镜也会留痕），分数低也不代表绝对干净；且 TruFor 只擅长查「局部被人动过手脚」，官方明确说明它查不出「整张完全由 AI 生成的图」

## 四、建议

1) 不要直接采信该图；2) 人工核验原始出处；3) 如用于举证，保留本报告与原始证据 JSON。

---

*本报告主体由模板从工具证据 JSON 直接拼装（结论可追溯到 `outputs/` 下的原始证据文件）。若存在「五、大模型辅助解读」段，则为模型基于证据生成的辅助说明，已通过禁用措辞校验，不替代规则引擎结论。*
