# 图片鉴定报告：clean_01

- 检测对象：`clean_01.png`
- 参与检测的工具：6 个
- **鉴定结论：无法判定（inconclusive）**

## 一、结论

> **没查出篡改信号，但也无法证明一定真实**
>
> 取证模型给出的整图篡改分为 0.3503，未达到可疑线；其余工具也没有发现明确篡改痕迹。注意：多数网络图片本来就没有可查凭证，「没查出问题」不等于「证明没问题」。
>
> **建议下一步**：如需要确证，建议向品牌方索取带 C2PA 凭证的原始原图。
>
> **提醒**：以上结论来自算法比对，不是法律意义上的鉴定意见。分数高不代表一定有罪（压缩、滤镜也会留痕），分数低也不代表绝对干净。

现有工具没有发现明确的篡改信号，但**也不能证明它一定真实**——多数网络图片本来就没有可查凭证。

判定依据（规则引擎给出）：
- AIGC 检测：AI 生成概率 0.9551（≥0.7），但本模型在本批素材上实测零区分度（真实图 0.9571 vs 篡改图 0.9621），仅作参考，不参与自动定级
- TruFor：篡改分数 0.3503（<0.5）→ 未发现明显篡改痕迹
- 现有工具未发现明确篡改信号，但也不能证明一定真实（多数网图本就无可查凭证）

## 二、检测依据（逐工具）

### 1. 内容指纹（哈希）

- 观察到：这张图的内容指纹是 sha256:660ae9ea3b8ab59b2ce4bc596e6fbc94320a118329e51f080c43c925cb50c5d8，文件大小 613123 字节
- 文件大小：613123 字节

### 2. 内容凭证（C2PA）

- 观察到：c2patool 没能读出凭证（绝大多数网图都没有凭证，属正常）
- 凭证状态：**missing（无凭证）**

### 3. 压缩痕迹分析（ELA）

- 观察到：整图平均误差水平 1.046；误差最高的区域 bbox=[0, 618, 93, 721]，该区域误差 1.685。注意：这张图的真实格式是 PNG，不是 JPEG。非 JPEG 转 JPEG 本身就会产生很大差异，本结果仅作演示、不可信。
- 整图平均误差：1.046
- 最可疑区域：bbox=[0, 618, 93, 721]，误差 **1.685**（触发阈值 2.0）
- 可疑区域总数：3

### 4. 图上文字识别（OCR）

- 观察到：在图上一共读出 1 行文字
- 识别内容：@雨弑_小红书

### 5. AI 生成检测（AIGC）

- 观察到：AI 生成概率 0.0（模型认为看起来像真实拍摄 / 人工制作的图）
- AI 生成概率：**0.0**（≥0.9 高风险，≥0.8 可疑）
- 模型标签：real

### 6. 篡改痕迹检测（TruFor）

- 观察到：TruFor 篡改分数 0.3503（有轻微篡改痕迹）；可疑区域约占全图 5.3%，主要集中在画面中部
- 整图篡改分数：**0.3503**（阈值：≥0.9 高风险，≥0.5 可疑）
- 可疑区域占比：约 5.3%
- 定位热力图：`outputs/clean_01_trufor_heat.png`
- 原图叠加图：`outputs/clean_01_trufor_overlay.png`

## 三、这些工具说不清什么（必读）

以下每一条都是对应工具**明确声明的能力边界**，超出这些范围的结论本报告不作数：

- **内容指纹（哈希）**：哈希只证明文件的身份，不能证明图片真伪、不能判断是否被篡改
- **内容凭证（C2PA）**：没有凭证不等于图片是假的；有凭证也不等于没被恶意编辑过
- **压缩痕迹分析（ELA）**：ELA 只是压缩痕迹分析，不能证明某处一定被篡改；截图、反复保存、平台压缩都会造成同样现象
- **图上文字识别（OCR）**：OCR 只是抄写员，不能证明文字内容为真，也不能证明图片被篡改
- **AI 生成检测（AIGC）**：模型也会误判：分数高不代表一定 AI 生成，分数低也不代表一定真实；本项与 TruFor 互补——TruFor 查局部篡改，本项查整图 AI 生成
- **篡改痕迹检测（TruFor）**：模型也会误判：分数高不代表一定有罪（压缩/滤镜也会留痕），分数低也不代表绝对干净；且 TruFor 只擅长查「局部被人动过手脚」，官方明确说明它查不出「整张完全由 AI 生成的图」

## 四、建议

1) 不能据此认定造假，也不能据此认定真实；2) 如需定论，补充原始出处、拍摄设备信息等外部证据。

---

*本报告由模板从工具证据 JSON 直接拼装，未使用大模型生成，所有结论均可追溯到 `outputs/` 下的原始证据文件。*
