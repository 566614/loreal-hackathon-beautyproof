# 图片鉴定报告：clean_02_recompressed

- 检测对象：`clean_02_recompressed.png`
- 参与检测的工具：6 个
- **鉴定结论：未出结论（NO_VERDICT）**

## 一、结论

规则引擎没有产出结论，请检查证据文件是否齐全。

## 二、检测依据（逐工具）

### 1. 内容指纹（哈希）

- 观察到：这张图的内容指纹是 sha256:4143141121f9f5a5c524a427d394a448777301db90d27cc0b9956c548cb51e69，文件大小 439869 字节
- 文件大小：439869 字节

### 2. 内容凭证（C2PA）

- 观察到：c2patool 没能读出凭证（绝大多数网图都没有凭证，属正常）
- 凭证状态：**missing（无凭证）**

### 3. 压缩痕迹分析（ELA）

- 观察到：整图平均误差水平 0.248；误差最高的区域 bbox=[186, 618, 279, 721]，该区域误差 0.71。注意：这张图的真实格式是 PNG，不是 JPEG。非 JPEG 转 JPEG 本身就会产生很大差异，本结果仅作演示、不可信。
- 整图平均误差：0.248
- 最可疑区域：bbox=[186, 618, 279, 721]，误差 **0.71**（触发阈值 2.0）
- 可疑区域总数：3

### 4. 图上文字识别（OCR）

- 观察到：在图上一共读出 2 行文字
- 识别内容：@雨弒

### 5. AI 生成检测（AIGC）

- 观察到：AI 生成概率 0.9989（模型给出了较高的 AI 生成概率，但该模型误判率很高（真实图也常被判高分），不能单凭此项下结论）
- AI 生成概率：**0.9989**（触发阈值 0.7）
- 模型标签：artificial

### 6. trufor

- 观察到：TruFor 篡改分数 0.4113（有轻微篡改痕迹）；可疑区域约占全图 1.0%

## 三、这些工具说不清什么（必读）

以下每一条都是对应工具**明确声明的能力边界**，超出这些范围的结论本报告不作数：

- **内容指纹（哈希）**：哈希只证明文件的身份，不能证明图片真伪、不能判断是否被篡改
- **内容凭证（C2PA）**：没有凭证不等于图片是假的；有凭证也不等于没被恶意编辑过
- **压缩痕迹分析（ELA）**：ELA 只是压缩痕迹分析，不能证明某处一定被篡改；截图、反复保存、平台压缩都会造成同样现象
- **图上文字识别（OCR）**：OCR 只是抄写员，不能证明文字内容为真，也不能证明图片被篡改
- **AI 生成检测（AIGC）**：模型会误判：真人也常被误判为 AI，AI 生成的也可能蒙混过关；分数不是事实
- **trufor**：模型也会误判：分数高不代表一定有罪（压缩/滤镜也会留痕），分数低也不代表绝对干净；且 TruFor 只擅长查「局部被人动过手脚」，官方明确说明它查不出「整张完全由 AI 生成的图」

## 四、建议

先补齐证据文件后重新生成报告。

---

*本报告由模板从工具证据 JSON 直接拼装，未使用大模型生成，所有结论均可追溯到 `outputs/` 下的原始证据文件。*
